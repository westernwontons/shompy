datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "cargo prisma"
  output   = "../ui/src/prisma.rs"
}

model User {
  id          Int    @id @default(autoincrement())
  displayName String
}
